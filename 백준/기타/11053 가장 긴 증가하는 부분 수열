N= int(input())
arr = list(map(int, input().split()))
ans=0
for start in range(N):
    cnt=1
    tmp =arr[start]
    for i in range(start, N):
        if arr[i] > tmp:
            cnt+=1
            tmp = arr[i]
    if cnt>ans:
        ans=cnt
    print(cnt)
print(ans)
